 Task 05 – Network Packet Analyzer 

This Python script captures live network traffic and analyzes packet data using Scapy.

 Features
- Displays source and destination IPs
- Identifies protocol (TCP/UDP/ICMP)
- Extracts raw payload (data)

 Requirements
- Python 3
- Scapy (`pip install scapy`)

  Note
May require **admin/root privileges** to run sniffing.

 How to Run
 On Windows:
Open **Command Prompt as Administrator**, then:

python packet_sniffer.py